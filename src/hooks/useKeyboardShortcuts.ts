/**
 * Keyboard shortcuts hook - CURRENTLY DISABLED
 *
 * All keyboard shortcuts are disabled to prevent interference with text input.
 * Mode switching and other actions are available via toolbar buttons.
 */
export function useKeyboardShortcuts() {
  // Keyboard shortcuts are completely disabled
  // All functionality is available through the UI toolbar buttons
}
